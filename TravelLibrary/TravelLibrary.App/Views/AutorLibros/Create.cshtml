@model TravelLibrary.App.Entities.AutorLibro

@{
    ViewData["Title"] = "Create";
}

<h2 class="alert alert-success">CREAR</h2>

<h4 class="alert alert-info">AUTOR Y LIBROS</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AutoresId" class="control-label"></label>
                <select asp-for="AutoresId" class="form-control" asp-items="ViewBag.AutoresId"></select>
            </div>
            <div class="form-group">
                <label asp-for="LibrosId" class="control-label"></label>
                <select asp-for="LibrosId" class="form-control" asp-items="ViewBag.LibrosId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Crear" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-success">Regresar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
